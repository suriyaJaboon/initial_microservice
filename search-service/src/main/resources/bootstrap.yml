spring:
  application:
    name: search-service
  cloud:
    config:
      uri: http://config:8888
#      fail-fast: true
#      password: ${CONFIG_SERVICE_PASSWORD}
      password: softnix
      username: user

eureka:
    client:
      serviceUrl:
        defaultZone: http://eureka:8090/eureka/
